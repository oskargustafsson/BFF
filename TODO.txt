Add input validation (of calling arguments) to all exposed functions
Conditional runtime checks https://github.com/mishoo/UglifyJS2#user-content-conditional-compilation
Docblocks
Only trigger 'prechange' events if there will actually be a change

MODEL
-----
Trigger three events on set; change:propname, change and *
Break out event into mixin
Use Object.defineProperties instead of Object.defineProperty
Check that properties not in the schema are not passed to the constructor (e.g. { namee: 'O' })
